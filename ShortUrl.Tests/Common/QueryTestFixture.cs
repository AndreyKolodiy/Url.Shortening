using Application.Common.Mappings;using Application.Interfaces;using AutoMapper;using Infrastructure;namespace ShortUrl.Tests.Common;public class QueryTestFixture{    public ShortUrlDbContext Context;    public IMapper Mapper;    public QueryTestFixture()    {        Context = ShortUrlContextFactory.Create();        var configurationProvider = new MapperConfiguration(cfg =>        {            cfg.AddProfile(new AssemblyMappingProfile(typeof(IShortUrlDbContext).Assembly));        });        Mapper = configurationProvider.CreateMapper();    }        public void Dispose()    {        ShortUrlContextFactory.Destroy(Context);    }        [CollectionDefinition("QueryCollection")]    public class QueryCollection : ICollectionFixture<QueryTestFixture> {}}